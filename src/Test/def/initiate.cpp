#include "..\..\Walnut\walnut.h"

std::set<Walnut::stats>* Walnut::Test::initiate() {

	// Start a test and create a report for individual section of the test.
	// Then combine all the individual reports into a single set of report type.
	// Just return the reference to the combined report.

}