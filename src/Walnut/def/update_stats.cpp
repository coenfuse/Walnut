#include "..\walnut.h"

void Walnut::update_stats(const std::string* of, Walnut::stats new_data) {

		// Updates the user progress report on system disk. Creates a new file if
		// no file is present on the disk

}