#include "..\walnut.h"

void Walnut::customize() {

	// Add a bounds check to prevent user from customizing a test if its already
	// in progress. i.e; You can't customize a test in between. Only before it.
}